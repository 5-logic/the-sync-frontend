import { createFullMockGroup } from './group';

// Student này thuộc group g1
export const studentGroup = createFullMockGroup('g1', 'Review 2');

// Student chưa có group
export const noGroupStudent = null;
