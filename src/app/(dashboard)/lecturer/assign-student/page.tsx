'use client';

import { Typography } from 'antd';

const { Title } = Typography;

export default function AssignStudentPage() {
	return (
		<div className="p-6">
			<Title level={2}>👨‍🏫 Assign Student Review</Title>
		</div>
	);
}
